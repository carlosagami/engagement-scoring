{
  "name": "poweremail-open-intelligence",
  "version": "1.0.0",
  "description": "Tenant-aware open telemetry and classification service for PowerEmail",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check scripts/provision-tracking-domains.js",
    "domains:sync": "node scripts/provision-tracking-domains.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
